# Cloudflare Pages redirect rules (path-based only).
# Docs: https://developers.cloudflare.com/pages/configuration/redirects/
#
# Note: Cloudflare Pages does NOT support host-based redirects in this file.
# Cross-host redirects (e.g., www.vvlfgng.dev -> vvlfgng.dev) live in
# functions/_middleware.js as a Pages Function instead.
#
# Add path-based rules below as needed in the future.
